# Cross-core change requests

This directory will contain materialized `cross_core_change_request` YAML files.

No request is materialized by PHASE_32.

The first expected candidate is:

```text
CCR_PATCH_LIFECYCLE_ESCALATION_THRESHOLD_N_R01
```

It must remain a future request until a human decision authorizes its creation.
